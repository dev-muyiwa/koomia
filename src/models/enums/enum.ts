
enum Role {
    USER = "user",
    ADMIN = "admin"
}

enum CategoryType {
    PRODUCT = "product",
    BLOG = "blog",
    BRAND = "brand"
}

enum VariantType{
    COLOR = "color",
    SIZE = "size"
}


export {
    Role, CategoryType, VariantType
}